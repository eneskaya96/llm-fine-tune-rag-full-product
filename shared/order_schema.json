{
  "$comment": "Canonical create_order contract. The serving layer parses and executes it and the frontend renders it; the corpus does not teach it and the evaluator does not score it, because which tools exist is prompt rather than weights. Change it here first.",
  "name": "create_order",
  "description": "Place the customer's order.",
  "parameters": {
    "type": "object",
    "properties": {
      "items": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Exact product name as written on the menu. Never invented."
            },
            "size": {
              "type": ["string", "null"],
              "enum": ["S", "M", "L", null],
              "description": "One of the sizes that product offers. null for food."
            },
            "milk": {
              "type": ["string", "null"],
              "description": "Named only when the customer asked for one, including when they name the default. null otherwise."
            },
            "extras": {
              "type": "array",
              "items": {"type": "string"},
              "description": "Extras the customer added. Anything they ruled out is simply absent."
            },
            "quantity": {
              "type": "integer",
              "minimum": 1
            }
          },
          "required": ["name", "size", "milk", "extras", "quantity"],
          "additionalProperties": false
        }
      }
    },
    "required": ["items"],
    "additionalProperties": false
  }
}
