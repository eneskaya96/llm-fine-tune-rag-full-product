# torch, gradio and spaces are already in the ZeroGPU image -- pinning them here
# fights the runtime rather than helping it. gradio's version is set by
# sdk_version in README.md.
transformers>=4.53.0
peft>=0.15.0
accelerate>=1.0.0
chromadb>=1.0.0
